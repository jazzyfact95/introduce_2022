import React from 'react';
import styled from 'styled-components';

const PortfolioCmpt = styled.div`
  width: 100%;
  height: fit-content;
  border-radius: 4px;
  border: 1px solid red;
`;

const Portfolio = ({ company, text }) => {
  return <Portfolio></Portfolio>;
};

export default Portfolio;
